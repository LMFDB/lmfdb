# Mathematical questions

Answers unblock the tickets listed in each header. Write answers directly under **Answer:** (any level of detail helps; "see preprint §x.y" is fine once it exists). Agents treat an empty Answer as "blocked" for the parts that need it.

---

## Q1. Legacy label format for models/points files — and where should Atkin–Lehner quotient data go? *(blocks T01, T02, T03)*

`~/claude/ShimCurve/data/models/lmfdb_shim_models.txt` (462 models) and `data/rational points/lmfdb_shim_rational_pt_updated.txt` (~424 point records) label curves like `26.1-[1]`, `26.1-[1,26]`, `94.1-[1,2]`. Working hypothesis: `D.N-[m1,m2,...]` means the quotient of X₀(D;N) by the Atkin–Lehner subgroup generated by the listed w_m (so `[1]` = X₀(D;N) itself, `26.1-[1,26]` = X₀(26;1)/⟨w₂₆⟩).

1. Is that reading correct? If not, what is the scheme?
2. Only the `[1]` curves have rows in `gps_shimura_test` today (the coarse X₀(D;N) rows). Proper AL quotients would, in the enhanced framework, be level-1 curves X_H for H ≤ Aut_{±μ}(O) ⋉ (O/1)ˣ — which are enumerated only for D=6 so far. Until Tier-3 coverage exists for the other discriminants: should we (a) upload only the `[1]` models/points now and park the quotient data, (b) generate the level-1 enhanced rows for exactly the ~48 discriminants that have models (needs T19/T20), or (c) something else?
3. For matching old→new: a `[1]` curve `D.N-[1]` should map to the unique row with discB=D, discO=D·N, deg_mu=1, level=1 — confirm that's the right target (e.g. `26.1-[1]` → the row `26.1.1.…` with order label `26`).

**Answer:**

---

## Q2. Precise definitions of `gerbiness`, `aut_gerbiness`, and `Gerby_gen` *(blocks T07; upstream issue #6)*

Current code has two independent computations:

- `enumerate-H.m` (`createRecord`): `gerbiness := #KG_level` where KG = image of `SemidirectToNormalizerKernel(O,mu)` at level N, and `aut_gerbiness := ` the number of distinct Aut-components among elements of KG.
- `enumerate-O.m:258-260` (`LMFDBRowEntry(O,mu)` → `Gerby_gen` column of `quaternion_orders_polarized`): hardcodes the identity, with the comment *"gerbiness = 1 because f: Aut_{±mu}(O) → N_{Bˣ}(O)/Qˣ is injective — this is only correct when the degree of polarization is 1! TODO: handle this more generally."*

Upstream issue #6 says the method "does not look at the projection onto the Aut_{±μ}(O) factor" and that gerbiness should be 1 exactly when deg μ = 1.

1. What is the intended mathematical definition of the `gerbiness` column? (Order of the kernel of X_H → its coarse space? Of Aut_{±μ}(O) → N_{Bˣ}(O)/Qˣ? Something else?)
2. Same for `aut_gerbiness` — and which of the two should the website display as "gerbiness"?
3. What should `Gerby_gen` contain for deg μ > 1 (a generator of which kernel, in what coordinates)?
4. Is the relation "gerbiness = 1 ⟺ deg μ = 1" a theorem we can assert as a sanity check?

**Answer:** (Eran, DECIDED — full text in [QUESTIONS_ANSWERS.md](QUESTIONS_ANSWERS.md) §Q2) Three
distinct quantities were sharing one name. `gerbiness` := |ker(f: Aut_{±μ}(O) → Aut(coarse curve))|
— the moduli-stack gerbe, trivial iff deg μ = 1. The old stored value (#KG_level, the root-of-unity
band) survives renamed `base_gerbiness`. `aut_gerbiness` is kept for the Gauss–Bonnet genus
normalization. Implemented in T07.

*Resolved (David, 2026-08-01, **D7** — see [DECISIONS.md](DECISIONS.md)):* the redefinition, the
`base_gerbiness` name and the schema growth are **approved as-is**, and **Q2.2's optional rename of
`aut_gerbiness` → `aut_band` is declined** — the column keeps its name. Note for readers of the data:
the new `gerbiness` coincides *numerically* with `aut_gerbiness` on the whole D=6 corpus (both count
the band whenever reduction mod N is faithful, which it always is at working level) even though the
two are semantically distinct.

*Still open (routes to Eran):* **D8** — whether the injectivity assert at `aut_mu_O.m:66` may stay
(T07 kept it deliberately, arguing it checks a different map than issue #6's); **D10** — confirming
that the "gerbiness = 1 ⟸ deg μ = 1" assert is intended to be maximal-order-only, with Eichler
orders spot-checked rather than asserted.

---

## Q3. Canonicality/uniqueness of the polarized element μ *(blocks T08; informs T06; upstream issue #5)*

`HasPolarizedElementOfDegree(O,d)` returns *some* μ with μ² + d·disc(O) = 0, via Magma's `Embed` (non-deterministic) with ad-hoc fallbacks. The polarized-order label is `discB.discO.deg_mu` — i.e., it assumes (O, μ) is determined up to the relevant equivalence by the degree alone.

1. Up to what equivalence is μ classified (Aut(O)-conjugacy? twisting as in `polarization-twisting.m`? Bˣ-conjugacy)? For fixed (O, d) can there be several inequivalent polarized μ of the same degree — and if so, should `quaternion_orders_polarized` have several rows for the same (order, degree), with labels disambiguated?
2. Is there a preferred canonical representative μ (e.g. minimal with respect to some explicit quadratic form on O⁰, ties broken lexicographically) that we should standardize on so that stored coordinates (`mu`, `AutmuO_generators`) are reproducible across runs?
3. Any preferred algorithm for finding μ deterministically on Eichler orders (where `Embed`/`InternalConjugatingElement` misbehave)? E.g. enumerate short vectors of the ternary form on the trace-zero submodule O⁰ with nrd = d·disc(O)?

**Answer:** (Eran, DECIDED — full text in [QUESTIONS_ANSWERS.md](QUESTIONS_ANSWERS.md) §Q3) μ is
classified up to **Pollack conjugation** (Rotger, Crelle 561 (2003)); a given (O, deg) admits
*finitely many inequivalent classes*, not one. T08 implemented complete canonical enumeration
(`pollack-classes.m`) with existence certified by the Eichler/Rotger class-number formula before any
search — no `Embed`, no `InternalConjugatingElement`.

*Resolved (David, 2026-08-01, **D31** — see [DECISIONS.md](DECISIONS.md)):* the **label cascade is
approved as proposed**. One `quaternion_orders_polarized` row per **negation pair** {[μ], [−μ]}
(= isomorphism class of degree-d polarizations); `mu_label` unified to the four-component
**`discB.discO.deg.i`** (maximal orders write discO = discB, so `6.6.2.1`); index `i` = position in
the canonical class order from `PolarizedElementClasses`; the added component cascades into the curve
label and widens the frontend `LABEL_RE`. **Not implemented anywhere yet** — scheduled as T27-time
work, coordinating with Q11 (open) and Q15. Migration is not blind: T08's audit already maps each
shipped row's stored μ to its true class index.

*Still open:* **D49** — whether `nrd_mu` (which satisfies `nrd_mu·deg_mu = discO` in the data, the
*reciprocal* of the gps convention `Norm(μ) = deg_mu·discO`) is a misnamed column or a scaled μ.

---

## Q4. Why does `assert #G/#G1plus eq 2` fail for D=6, N=5? *(blocks T10, T23)*

In `enumerate-H.m` (both `EnumerateH` and the `GetG1plus` path), G = image of Aut_{±μ}(O) ⋉ (O/N)ˣ in GL₄(ℤ/N), and G1plus = subgroup generated by the images of `NormalizerPlusGeneratorsEnhanced(O,mu)`. The code asserts [G : G1plus] = 2, with the comment *"This assertion fails for N = 5, D = 6. Do we understand why?"* — and N=5 is consequently absent from all generated data.

1. Is [G : G1plus] = 2 actually expected for all N coprime to D (norm-sign index), or does the expected index depend on N (e.g. on whether −1, or the norms of the AL elements, are squares mod N — note 5 is the first prime ≡ 1 mod 4 in the tested set)?
2. If the index legitimately varies, what is the correct definition of the "plus" subgroup at level N that the enumeration should use?

**Answer:**

---

## Q5. Fine vs. coarse: the right criterion, and whether it can occur for gerbiest H *(blocks T11)*

Every row currently hardcodes `is_coarse = true`, and `upload_scripts/shimcurve_generate.py:19` has *"TODO: needs a way to tell if -1 is in the group (coarse/fine moduli)"*. The frontend already parses fine labels (hyphenated, modular-curves style) and merges coarse data into fine pages.

1. What is the correct criterion in the enhanced setting — (1, −1) ∈ H ≤ Aut_{±μ}(O) ⋉ (O/N)ˣ? (−1 meaning the image of −1 ∈ (O/N)ˣ.)
2. The main enumeration only produces "gerbiest" H (containing the full kernel KG). Does KG always contain (1, −1) — i.e., is every gerbiest H automatically coarse, so fine curves only appear once we enumerate non-gerbiest H?
3. What should the fine-label format be? (Frontend regex expects `{coarse part}-{fine part}` à la modular curves: `main.py:59`.)

> **[agent wave3-N data, 2026-07-22]** Empirical input for sub-question 2 (data, not an answer):
> `ContainsMinusOne` ((1,−1) ∈ H, tested as membership of `EnhancedElementInGL4modN(⟨B!1, −O!1⟩, N)`
> = −I₄, ShimCurve branch `ticket/T11-fine-coarse-labels`) was computed in a fresh enumeration pass
> over the whole D=6 corpus parameter set — deg μ ∈ {1,2,6} × N ∈ {1,2,3,4,6} (the 2,198 shipped
> rows) plus deg 1, N=5 (175 rows) — **all 2,373 gerbiest H contain (1,−1); 0 fine**. The
> structural reason was checked mechanically at every (deg,N): (1,−1) lies in the reduced kernel
> KG returned by `SemidirectToNormalizerKernel`, and the enumeration only builds H ⊇ KG. So the
> shipped `is_coarse = true` hardcode is empirically confirmed correct for every existing row, and
> fine curves can only arise from a future non-gerbiest enumeration.

**Answer:**

---

## Q6. `scalar_label` convention *(blocks T11)*

`enumerate-H.m:385-386` builds `scalar_label = "{level}.{scalar_index}.1"` with the comment *"At the moment we are not sure how to label the scalar subgroup."* What should the final component(s) be — is there a canonical enumeration of scalar subgroups of (ℤ/N)ˣ (as in the modular-curve `scalar_label`), and what does the website need it for?

**Answer:**

---

## Q7. Elliptic-point counting for general D *(blocks T20)*

`EnhancedEllipticPoints` (genera.m:18) is documented *"Only works for discriminant 6!"*: it assumes the ramification data σ is indexed by points of orders `bottom := [2,4,6]` — i.e., that the bottom curve (quotient of X(D;1) by the full enhanced normalizer-plus group) is the (2,4,6)-triangle quotient, which holds for D=6, deg μ=1.

1. For general (D, μ), what is the signature of the bottom orbifold X(D;1)^{Aut_{±μ}(O)}, and is there a formula/algorithm you want used (Ogg-style counts as in `X0DN_code.m`, extended to AL quotients — cf. `SignatureX0DNmodAtkinLehnerElement`; or compute the Fuchsian signature of the group generated by Γ₀(D)⁺-type elements directly)?
2. In particular, is the bottom always genus 0 for the discriminants we plan to cover, and if not, how should `EnhancedRamificationData`/`EnhancedGenus` handle positive-genus bases (the Riemann–Hurwitz code already allows it; the ν-bucketing does not)?
3. The ν-columns are (ν₂, ν₃, ν₄, ν₆). Can other orders occur (ν₅? ν₁₂?) for the curves in scope, and should the schema change to a list of (order, count) pairs instead?

**Answer:**

---

## Q8. Generators of the positive-norm normalizer for general D *(blocks T19)*

`NormalizerPlusGenerators(O)` (elliptic-elements.m) returns hardcoded generator lists for D ∈ {6, 10, 15} and the string "oops, not written for this discriminant yet" otherwise. To scale, we need, for each maximal (later Eichler) order O: generators of N_{Bˣ}(O)⁺/Qˣ, preferably as a fundamental-domain-compatible set from which elliptic elements can be read off.

1. Is it acceptable to compute these via Magma's `FuchsianGroup(O)` machinery (fundamental domains à la Voight) — slower but general — caching results to disk? Or do you have a preferred algebraic construction (e.g. explicit AL lifts + unit-group generators)?
2. The current hardcoded lists were chosen so that the *enhanced* lifts behave well (`NormalizerPlusGeneratorsEnhanced` wraps them with μ-twists). Any constraints on the choice of generators that the general code must preserve (e.g. that each generator normalizes O and has positive norm, that AL representatives have norm exactly m | D)?

**Answer:**

---

## Q9. Obstructions / pointless / point-count semantics *(blocks T15)*

Frontend reads `obstructions` (integer[]), `has_obstruction` (smallint: 1/0/−1?), `pointless` (boolean), `num_known_degree1_points`, `num_known_degree1_noncm_points`, `cm_discriminants` — all copied from the modular-curves schema where `obstructions` lists places (0 = ℝ) with no local points.

1. Confirm the conventions should match modular curves (0 encodes the real place; primes encode ℚ_p-obstructions).
2. Which theorems should the first pass encode? Candidates: Shimura's theorem that X₀(D;N)(ℝ) = ∅ for D > 1 (applies to every curve in the table that covers X₀(D;N)? — but AL-type quotients X_H with H containing Aut-elements can have real points); Ogg/Jordan–Livné local-point criteria at p | D; rational CM points from `X0DN_code.m` (`RationalCMPointsX0DN`, `RationalCMQuotientsX0DN`) giving lower bounds for quotient curves.
3. For which H exactly does "X_H covers the coarse X₀(D;N)" argument apply — is the right statement "H ∩ ({1}⋉(O/N)ˣ) ≠ full ⟹ …" or should we just test whether the Aut-projection of H is trivial?

**Answer:**

---

## Q10. Sources for exact gonalities *(informs T17)*

`enumerate-H.m:529-540` sets gonality bounds with the comment *"These could be better — maybe (g+3)/2? Ask Oana and Freddy."* (Padurariu & Saia have work on gonalities of Shimura curves and their AL quotients.)

1. Should we import their tables (if shareable) as the source for exact `q_gonality`/`qbar_gonality` where known?
2. Is the improved general upper bound ⌈(g+3)/2⌉ (over ℚ̄, genus ≥ 2) safe to apply here, and any better D-specific bounds you want encoded?

**Answer:**

---

## Q11. Naming conventions for the `name` column *(blocks T18)*

391 rows currently have names. What is the intended scheme — which curves get names, and in what syntax? (Observed in frontend: `X(D;N)`-style, `X*(...)`, `X0(D;N)`; jump box also accepts `X(D,M;1)` for Eichler orders and fiber products via `*`.) Please spell out the full grammar (families: X₀, X₁, X, X*, X_{sp}?, Eichler-order forms, polarization degree in the name?) so names can be generated systematically and `canonicalize_name` (web_curve.py:98) can be checked against it.

**Answer:**

---

## Q12. Scope decisions for the first public release *(informs T21–T23, T27)*

1. Target coverage: which (D, deg μ, N) ranges should the release aim for? (E.g. all D ≤ 100 maximal at N=1; D=6,10,15 with N ≤ some bound; Eichler orders?)
2. Non-gerbiest H: in or out for v1?
3. Final table names: `gps_shimura_test` → ? (`gps_shimura`? `shimcurve_curves`?) — and should `quaternion_orders`/`quaternion_orders_polarized` keep their names?
4. Completeness statements for the rcs knowls: what can we honestly claim ("all gerbiest surjective H for D=6, N ≤ 6, N ≠ 5", etc.)?

**Answer:**

---

## Q13. Jacobian-column semantics + cmfdata provenance *(informs T13, T14)*

1. Confirm the Jacobian columns should copy modular-curve semantics: `rank` = **analytic** rank of Jac(X_H) (sum over newforms of mult · analytic rank), `conductor` = factored conductor, `traces` = a_p list of the Jacobian's L-function, `trace_hash` = standard LMFDB trace hash, `dims`/`mults`/`newforms` aligned and sorted by CMF label.
2. `jacobian_decomp/helpers.m` expects an external `cmfdata.txt` (not in the repo). Is there an existing dump script/format doc, or should T13 write one from `db.mf_newforms` (which columns beyond label/level/weight/dim/traces/AL-signs does the trace-matching need)?
3. The Jacquet–Langlands transfer used: for X_H at level N over the Eichler order of level M, the relevant newforms are D·N·M-new of level dividing D·N·M — confirm the exact space so the matching cutoffs are right.

**Answer:**

---

## Q15. Canonical ordering of subgroups H → the label tiebreaker *(blocks T29)*

**Measured problem**: running `GenerateDataForGerbiestSurjectiveH(O, mu, 1)` for D=6, deg μ=1 twice in a row assigns the labels `6.1.1.2.0.a.1` / `6.1.1.2.0.b.1` to the *opposite* two curves each time. `updateLabels` (`enumerate-H.m:288`) sorts by `PermutationCharacter` and the two curves tie, so the class letter is decided by whatever order `Subgroups(G,KG)` happened to return. The committed data disagrees with both runs. Since LMFDB labels are permanent identifiers, we need a canonical total order.

The two tied curves are distinguishable: `autmuO_norms = {6,1,1,1,1,1}` with generators `{{2,-2,-3,1,0,0,0,0},…}` vs `{3,1,1,1,1,1}` with `{{-1,0,1,1,0,0,0,0},…}`.

1. What should break the tie, mathematically? Modular curves carry an explicit `tiebreaker` column (and the shimura frontend already lists `tiebreaker` among copied-but-unused modcurve columns) — do we want the analogous notion, and what is it here? Candidates: the Atkin–Lehner data (sorted `autmuO_norms` — natural, since these are the refined AL involutions and would give the "w₆ curve" a stable letter relative to the "w₃ curve"); the generator coordinates under a fixed lex order (canonical but basis-dependent and ugly); the fixed-field/CM data of the corresponding quotient.
2. Should the ordering be intrinsic (invariant-based, so independent of the choice of μ and of Magma's enumeration) or is a fixed-choice-of-μ + fixed-algorithm reproducibility enough? (Intrinsic is much more robust — it survives T08's change of μ representative — but needs a complete invariant to sort on.)
3. Practical consequence: the shipped labels in `gps_shimura_test` were produced by the current unstable procedure, so they may not be reproducible at all. Is it acceptable to **relabel the whole table** on the next reload (breaking any existing external references), or must we pin the existing labels and only stabilize going forward? T29 step 4 measures how many rows would actually move.

**Answer:** (David, 2026-07-16, in-session) We should pick a canonical sort. The first tiebreaker should be the Atkin–Lehner content, and the second should be a new implementation of canonical generators (see `GL2CanonicalGenerators` in https://github.com/AndrewVSutherland/Magma/blob/main/gl2base.m for the GL2 case).

*Status:* implemented in T29 (branch `ticket/T29-label-determinism`); see that ticket's Log for the precise spec (AL content = sorted multiset of squarefree Aut-component norms over cosets of H∩(O/N)ˣ; canonical generators = greedy lex-minimal generating sequence minimized over the G-conjugacy class, computed lazily on ties). Full-corpus determinism verified 2026-07-17.

*Resolved (David, 2026-08-01 — see [DECISIONS.md](DECISIONS.md)):*
- **15.3 SIGNED OFF (D5).** The shipped assignment is not reproducible (73% of rows change curve under the canonical sort, psl2label on 98%), so pinning old labels was never an option: **T27 is a full atomic `copy_from` reload with the rename, and the 304 `shimcurve_pictures` rows re-key at reload.** Label-keyed `update_from_file` against the current table is permanently unsafe; every `PROVISIONAL` artifact stays parked until T27 runs.
- **Both semantic fixes BLESSED**: `is_split` = "some conjugate of H splits against the standard section" (**D3** — the old computation was representative-dependent and flipped between identical runs), and `G1` = the kernel of the reduced-norm determinant hom on G (**D4** — the old value was an arbitrary maximal det-trivial subgroup from an unstable enumeration).

*Still open for David/Eran:*
- Element ordering inside canonical generators is plain lex on matrix entry sequences (simplest restatable spec), not Sutherland's similarity-class stratification — flag if you want the latter before labels freeze.

---

## Q14. Area normalization discrepancy *(part of T06)*

`Area(O)` (enumerate-O.m:34) = φ(D)ψ(M)/12; for the maximal order of D=6 this gives 1/6, which is what `quaternion-orders.txt` contains — but `quaternion-orders.m` has 1/3 for the same order (factor 2). Which normalization is intended for the LMFDB column (hyperbolic area/(2π) of X₀(D;M), i.e. Vol/(2π) = φψ/6? or the Euler-characteristic normalization φψ/12?), and should the column be renamed (`area` vs `chi`) to make the convention explicit?

**Answer:**
