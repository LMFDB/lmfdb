---
id: T21
title: Run the enhanced enumeration for D = 10 and D = 15
status: open
owner: none
priority: P2
tier: 3
repos: [ShimCurve, db-readonly]
depends_on: [T07, T09, T19, T20]
questions: [Q12]
---

## Context

First coverage extension: D = 10 and 15 already have (hardcoded) normalizer generators, so they run as soon as elliptic points (T20) and the correctness fixes (T07 gerbiness, T09 Aut cases; ideally T10/T11 so labels don't churn twice) land. Scope per Q12.1 — default proposal: all deg μ | disc with a polarized element, N ∈ {1,2,3,4} ∪ {small primes coprime to D} matching the D=6 pattern, plus whatever Q12 says.

The README driver (adapted):
```
D := 10;  // then 15
B := QuaternionAlgebra(D);
O := MaximalOrder(B);
for deg in Divisors(D) do
  tr, mu := HasPolarizedElementOfDegree(O, deg);
  if not tr then continue; end if;
  for N in [1,2,3,4,6] do   // per Q12; remember N=5 needs T10
    subs := GenerateDataForGerbiestSurjectiveH(O, mu, N);
    WriteHeaderAndSubgroupsDataToFile(subs, O);
  end for;
end for;
```
D=6 timings ran 5s–33min per (deg,N); expect worse at higher D (Subgroups(G, KG) grows) — run in background with per-case timing logs.

## Steps

1. Preflight: confirm T19/T20's D=10/15 oracle tests pass; confirm which deg μ exist (`HasPolarizedElementOfDegree` for each divisor; cross-check against `quaternion_orders_polarized` rows for orders 10, 15).
2. Run the driver per D; capture timings; watch for assertion failures (each one is a finding — log it, don't bypass).
3. Sanity battery over the output files: coarse-row genus vs `SignatureX0DN`; Σ over lattice levels consistent; label uniqueness; area assert count; row counts recorded.
4. The X₀(D;N)-coarse rows for D=10,15 already exist in the DB from the signature-table arm — the new enhanced rows must **collide correctly**: same label ⟹ same invariants. Run the comparison (join staged file against devmirror rows) and reconcile: the enhanced rows carry more columns; any *disagreement* on shared columns (genus, index, ν's) is a release-blocking bug in one arm — investigate before staging.
5. Stage upload files + `update_from_file`/`copy_from` commands in the Log (new rows + enriched existing rows).

## Acceptance criteria

- Data files for every viable (D ∈ {10,15}, deg, N) in scope; failures itemized.
- Cross-arm collision check clean (or discrepancies resolved with a root cause).
- Timings table in the Log (informs Q12 scope for D beyond 15).

## Log

- 2026-07-16: ticket created from survey.
