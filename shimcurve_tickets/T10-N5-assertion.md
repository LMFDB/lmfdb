---
id: T10
title: Diagnose the D=6, N=5 index-2 assertion failure
status: review
owner: wave1-A-fable
priority: P1
tier: 1
repos: [ShimCurve]
depends_on: []
questions: [Q4]
---

## Context

`code/level-structure/enumerate-H.m` (inside `EnumerateH`, ~`:676-678`; the `GetG1plus` path used by `GenerateDataForGerbiestSurjectiveH` builds the same objects at `:40-47`):

```
G1plus:=sub< G | NBOplusgensGL4 >;
// This assertion fails for N = 5, D = 6. Do we understand why?
assert #G/#G1plus eq 2;
```

G = image of Aut_{±μ}(O) ⋉ (O/N)ˣ in GL₄(ℤ/N); G1plus is generated by the GL₄-images of `NormalizerPlusGeneratorsEnhanced(O,mu)` (`polarization-twisting.m:157`, wrapping the hardcoded `NormalizerPlusGenerators`, `elliptic-elements.m`). This is why **N=5 is missing from all generated data** (levels shipped: 1,2,3,4,6).

Empirical fact to explain: fails at N=5, passes at N=3,4,6 (and at 3·1, 3·2 via the level-lifting trick for N=1,2). 5 is the first tested prime ≡ 1 (mod 4) — plausibly the sign/square obstruction that makes the "plus" part surject or collapse mod N differs there (e.g. whether −1 or the norms of AL elements are squares mod N).

## Task

Determine mathematically what [G : G1plus] is for D=6 at N=5 (compute it: is it 1? 4?), why, and what the correct level-N notion of the plus-subgroup is; then fix the code so all N coprime to D work uniformly.

## Steps

1. Reproduce in a Magma session: build G and G1plus for (D,μ,N) = (6, deg 1, 5); print `#G, #G1plus`, the reduced-norm/determinant images, and which generators of G are missing from G1plus. Repeat at N=7, 11, 13 (both residue classes mod 4) and N=5 with deg 2, 6 — the pattern across N mod 4 (or mod something) is the diagnostic. Record the table in the Log.
2. Formulate the hypothesis (e.g. "G1plus = kernel of the composite G → (ℤ/N)ˣ/± → …; index = 2 iff χ(N)=… "). Cross-check against Q4's answer if present; if Q4 is unanswered, write the empirical findings **into QUESTIONS.md under Q4** as data for David.
3. Implement the fix per the resolved definition: either the assertion becomes a computed quantity (and downstream code — `quo<G1plus|KG>`, coset representations, `EnhancedRamificationData` — is checked to not secretly assume index 2), or the generator set is corrected so index 2 is restored. Grep for other places assuming index 2 (`#G/#G1plus`, `#G/2`, comments about plus).
4. Validate: rerun the D=6 deg-1 enumeration at N=3 and N=4 and diff against `data/genera-tables/genera-D6-deg1-N{3,4}.m` — must be identical (no regression), then run N=5 and sanity-check a few rows (genus of X₀(6;5): compare with `SignatureX0DN(6,5)` from `X0DN_code.m`; the coarse curve must appear with matching genus/index).
5. Do **not** mass-generate other levels here (that's T23); just leave the pipeline N=5-capable, with the N=5 deg-1 file generated as evidence.

## Acceptance criteria

- The Log contains the index table across N and the accepted explanation (or points to Q4's answer).
- N=3/4 regeneration is bit-identical to shipped data; N=5 file generates without asserts and passes the coarse-curve cross-check.
- `tests/run_quick.m` green.

## Log

- 2026-07-16: ticket created from survey.
- 2026-07-22 (wave1-A-fable): **DONE per Q4's decided answer** (index = φ(N), reduced-norm quotient G ↠ (Z/N)ˣ with kernel G1plus = Aut ⋉ (O/N)¹). Branch `ticket/T10-N5-assertion` (worktree tier1core, stacked on T29→T28), commit `246834d`.
  - **Fix**: `assert #G/#G1plus eq 2` → `eq EulerPhi(N)` at all three sites — `enumerate-H.m` GetG1plus (was :45), `enumerate-H.m` EnumerateH (was :677, now :772 after T28/T29 shifts), `genera.m` EnhancedCosetRepresentation (:66, `#G/#Gplus`). Mechanism recorded as docstring on GetG1plus + comments at the other two sites.
  - **Repo-wide index-2 audit (Q4's ⟐ confirmation)**: grepped `#G/#G1plus`, `#G/2`, `eq 2`, `index 2`, `Gplus` across `code/` and `tests/` — **only the three assert sites assume index 2**; nothing downstream consumes the index (Fuchsian index computed independently via `#G1plusmodKG/#H1plusquo`). The `X0DN_code.m` "index 2 subfield" hits are CM ring-class-field statements, unrelated. Confirms Q4's "only those three".
  - **Baseline correction (T29-era)**: `git log --stat da5540b..HEAD -- data/` shows T29 committed **no** data files, so the shipped genera files in-tree still carry old nondeterministic labels; shipped-vs-regenerated differs in every row (expected, per T29's 73% reconciliation). Baseline therefore self-generated pre-fix at T29 state.
  - **No-regression proof**: D=6 deg-1 N=3 (36 rows) and N=4 (262 rows) regenerated pre-fix and post-fix — **identical in all 66 canonical fields**; only `generators` (field 23) and `ram_data_elts` (field 60) differ. Confirmed pre-existing process-level variance, not caused by the fix: two *identical-code* post-fix runs differ in exactly the same two fields (matches T29 Log's known follow-up: Ngens rep / coset-numbering Lehmer codes are presentation-dependent).
  - **Q4 spot-check at N=5**: #G = 1920, #G1plus = 480, [G:G1plus] = 4 = φ(5); G/G1plus abelian invariants [4] ≅ (Z/5)ˣ — matches Q4's table (index 4 at N=5).
  - **N=5 evidence file**: `data/genera-tables/genera-D6-deg1-N5.m` generated with no assert failure — 175 rows × 68 fields, 3.5 s. **PROVISIONAL — pending T27 reload** (labels from the canonical T29 sort; a label-keyed update against the current DB is unsafe).
  - **Coarse cross-check**: `SignatureX0DN(6,5)` = (genus 1, e₂ = 4, e₃ = 0); the unique trivial-Aut-projection row at fuchsian_index 24 (= ψ(5)·#Aut_{±μ} = 6·4 over the (2,4,6) bottom), label `6.1.5.24.1.b.1`, has genus 1, ν₂ = 4, ν₃ = 0, ν₄ = ν₆ = 0 — exact match.
  - **Verification**: `tests/run_quick.m` green (0 failures, 0 skips). N3/N4 data files restored to committed state (regeneration was baseline validation only; wholesale relabeling of committed data = T27 scope, consistent with T29's choice).
  - **Flags for David**: (1) Q4's ⟐ sign-off — my grep confirms only the three assert sites; (2) N=5 index profile for the record: 175 gerbiest surjective classes, indexes 2–240, genus 0–11; (3) the N=5 file is committed as PROVISIONAL evidence only — mass level generation stays in T23.
