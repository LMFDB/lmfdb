---
id: T08
title: Robust + canonical polarized-element computation (upstream issue #5)
status: review
owner: wave1-B-fable
priority: P0
tier: 1
repos: [ShimCurve]
depends_on: []
questions: [Q3]
---

## Context

Upstream issue assaferan/ShimCurve#5: `HasPolarizedElementOfDegree` (`code/level-structure/polarization-twisting.m:71`) is ad hoc. Problems: Magma's `Embed` is non-deterministic and sometimes returns μ with enormous coefficients; `InternalConjugatingElement` errors on non-maximal Eichler orders; the Eichler fallback is a naive point search. Consequences: irreproducible stored `mu` / `AutmuO_generators` coordinates (cf. T06's 800-vs-804 row drift), fragility that blocks Eichler-order level structure (T22), and slow/failed searches at larger discriminant.

A μ of degree d is an element of O with μ² + d·disc(O) = 0, i.e. a trace-zero element with nrd(μ) = d·disc(O).

## Task

A deterministic, always-terminating (within the target range), canonical computation of μ for maximal and Eichler orders, per Q3's canonical-form decision.

## Steps

1. Read Q3. If no canonical form is prescribed, propose one in this ticket's Log and get sign-off before mass regeneration (suggested default: enumerate trace-zero short vectors of the positive-definite form nrd on O⁰ ∩ (lattice), take the first vector of norm d·disc(O) under a fixed ordering — deterministic by construction).
2. Implement: restrict nrd to the rank-3 trace-zero sublattice O⁰ (basis via `TraceZeroSubspace`-style computation from the O-basis), run `ShortVectors` / `ThetaSeries`-guided enumeration up to d·disc(O), filter nrd = d·disc(O) exactly. This avoids `Embed` entirely. Keep the old path behind a flag for comparison.
3. Preserve the intrinsic's contract: same signature and return convention (`tr, mu`), same downstream expectations (`DegreeOfPolarizedElement(O,mu)` = d at `:154`; `IsTwisting`; `Aut(O,mu)` — run these on the new μ for the full D ≤ 1000 maximal range as a consistency sweep).
4. Handle the twisting subtleties: `polarization-twisting.m:196` has "TODO: not sure if this is enough when O is not maximal to conclude element is in normalizer" — resolve for Eichler orders (the normalizer condition must be *checked*, not assumed; add the check).
5. Determinism test: run the sweep twice, assert identical μ coordinates. Speed: record timings; D ≤ 1000, all degrees, should be minutes not hours (short-vector enumeration in rank 3 is fast).
6. Regenerate `quaternion-orders-polarized` files; expect coordinate-level diffs everywhere (that's the point) — verify invariants (`deg_mu`, `nrd_mu`, `AutmuO_size`, `AutmuO_label`) are unchanged row-by-row vs old files, and flag any row where they differ (that would be a real bug being fixed — investigate each).
7. Tests: extend `tests/smoke_intrinsics.m` with a determinism assertion; `run_quick.m` green. Stage reload commands in the Log.
8. Draft (don't post) the upstream issue #5 resolution comment.

## Acceptance criteria

- No call path into `Embed`/`InternalConjugatingElement` remains in polarized-element computation (grep proof).
- Determinism sweep passes; invariant comparison old-vs-new documented.
- Eichler orders: `HasPolarizedElementOfDegree` succeeds for all Eichler orders with discO ≤ 1000, deg 1 (the current 86-row range) and a sample of higher degrees.

## Log

- 2026-07-16: ticket created from survey.
- 2026-07-22 (wave1-B-fable, claim): worktree `/Users/roed/claude/shim-wt/T08`, branch `ticket/T08-polarized-element` on T29 base 97bef4f. Q3 (DECIDED) supersedes the ticket's "canonical single mu" framing: polarized elements are classified by Pollack conjugation; deliverables reordered accordingly.
- 2026-07-22 (theory extraction, Rotger arXiv:math/0211120): pulled the paper (pp. 4–21). Load-bearing statements: Def 2.3 (mu1 ~ mu2 iff mu1 = conj(a)·mu2·a, a in O^x), Def 3.6/Cor 3.7 (classes <-> line bundles), Prop 4.1 (p(S,O) = |n(O^x)/N_{L/F}(S^x)|·e(S,O)/2), Lemma 4.2 (each ±Eichler class holds ε_mu·2^{e_S−1} Pollack classes), Thm 5.1/Rem 5.2 (index i(L)+i(L^{-1})=2, i ∈ {0,2} for δ>0 ⟹ [mu] and [−mu] never conjugate over hereditary O ⟹ classes pair, polarizations = pairs). Over Q: n(O^x) = {±1} (HSM, indefinite), N(S^x) = {1} ⟹ **#classes(nrd = n) = Σ_{S ⊇ Z[√−n]} e(S,O) = Σ_f h(S_f)·∏_{p|D}(1−{S/p})·∏_{p|M}(1+{S/p})** (Eichler symbols; M squarefree). Valid for EVERY norm n, not just n = d·discO — this generality became load-bearing for the algorithm (content strata below).
- 2026-07-22 (**paper erratum worth reporting**): Rotger's Section-7 example claims π₀(A) = 1040 for D = 2·3·5·7·11·13·17·19. This is arithmetically impossible: π₀ = h(−4D)/2 here, and genus theory forces 2^7 | h(−4D) while 2·1040 = 2080 = 2^5·5·13. Magma (two independent methods: QuadBin and number-field) gives h(−38798760) = 2048 = 2^11, so π₀ = **1024**. Likely a 2001-era PARI class-number artifact. Not load-bearing for us (small anchors D=6,10 → 1 principal polarization, D=15 → 2 all match the literature), but David may want to pass it to Rotger.
- 2026-07-22 (algorithm, after two dead ends — both documented in pollack-classes.m's header): canonical key = **(nrd/content², sup-norm, lex)** on the echelon trace-zero basis of Basis(O). The content (gcd of coordinates) is a Pollack-class invariant; strata by content reduce to problems nrd = n/e² on the same lattice, certified empty/nonempty by the count formula, enumerated in sup-norm rings by an exact Δ-pruned conic fibration. Dead end 1: plain sup-norm boxes (no strata) — D=206 deg 206 took 23 min (its minima are 103·(nrd-4 elements); the primitive-looking search space is astronomically large). Dead end 2: positive-definite majorant shells via ShortVectors — instant on D=206 but uncomputable on D=298 deg 1 (primitive stratum, skew echelon basis: sup-norm-4172 minimum has majorant height ~10^8; >80 s and climbing). Strata + rings: both run in ≤ 1.7 s. The Pollack test IsPollackConjugate is exact and finite: conjugator lies in the rank-2 lattice V_eps ∩ O (V_eps = {x : x·mu1 = eps·mu2·x}), nrd definite there (division algebra), ShortVectors with bound 1.
- 2026-07-22 (T09 intel, both directions): (a) received agent A's report that legacy HasPolarizedElementOfDegree returns a NON-normalizing mu for discO=30 deg 30, crashing IsTwisting → NormalizingElementToGL4 (embed-in-GL4.m:51). Root cause is structural: for gcd(deg, M) > 1 some Pollack classes normalize and some do not (e.g. (D,M,deg) = (6,5,5): the 4 conductor-1 classes normalize, the 16 conductor-5 ones do not), and for deg ∤ discO NO class normalizes (odd valuation at a split place). The canonical path now (i) returns false unless deg | discO, (ii) proves+asserts normalizing for gcd(deg,M)=1, (iii) filters classes by the (class-invariant) normalizing flag for gcd(deg,M)>1. Covered in tests/smoke_intrinsics.m (discO=30 deg-30 regression: normalizes + IsTwisting runs). (b) For T07's per-class gerbiness: for (D,deg)=(6,2) BOTH Pollack classes have S_mu = Z[ω] (disc −3, ζ₆ band; mu = 2·(nrd-3 element)); a ζ₄-band deg-2 mu is impossible over Q(√−12) ⟹ any shipped deg-2 row carrying ζ₄-band data is stale/wrong, not a class choice. PolarizedElementClasses returns per-class conductor/discS/content for T07 to consume.
- 2026-07-22 (**environment finding — Eichler orders are not reproducible from (D,M)**): `QuaternionOrder(B, M)` returns a DIFFERENT Eichler order/basis per process (verified: 3 runs, 3 different bases for (6,5), (6,35)); `MaximalOrder(B)` is stable. The canonical mu is canonical relative to (O, Basis(O)); reproducibility for Eichler rows therefore REQUIRES reconstructing O from the basis stored in quaternion-orders.txt (the regen driver and Eichler sweep do this; EnumerateOmuTxt's ord_data path already did — but its fallback `QuaternionOrder(B, M)` when the file is missing is nondeterministic; flag for agent A/T06).
- 2026-07-22 (**800-vs-804 row drift explained**, T06 evidence): quaternion-orders-polarized.m (800 rows) vs quaternion-maximal-orders-polarized.txt (804): the .m file is missing exactly 33.1, 33.11, 33.33, 34.1. The count formula says all four exist (D=33: Σ = 4 classes at deg 1 etc.), so the .m generation run's legacy path failed/was interrupted there and resumed at 34.2 — precisely the issue-#5 fragility. Also duplicate-signature bug in enumerate-O.m: two `EnumerateOmuTxt(RngIntElt)` intrinsics (lines 328/437), the second (maximal-only) shadows the first (the one with Eichler support + resume) — flag for agent A.
- 2026-07-22 (commits so far): c33673a (WIP: pollack-classes.m, HasPolarizedElementOfDegree canonical+Legacy, IsTwisting TODO resolution + two-value returns, smoke/sweep/regen scripts), 0eeaa9b (Eichler higher-degree acceptance script), fbac0d3 (final strata+ring engine). tests/run_quick.m green (0 failures) including the new T08 smoke block. Determinism sweeps + regeneration audit in progress.

- 2026-07-22 (engine, final iteration + performance war story): two more pathologies surfaced and were fixed before the full sweep could pass: (i) primitive strata over skew echelon bases (D=298 d=1, sup-norm-4172 minimum) killed the majorant-shell variant → final spec enumerates sup-norm rings, and the trace-zero basis is now CANONICALLY REDUCED (echelon kernel basis, then LLL w.r.t. the canonical majorant — deterministic; D=326's Gram went from entries ~3260 to ~132 and gained a definite principal minor); (ii) per-fibre CloseVectors cost 39 ms — replaced by a direct integral ellipse solve (congruence-restricted interval + quadratic), ~1000×. Where no principal 2×2 minor of the reduced Gram is definite, fibration_data builds a unimodular fibration from OrthogonalizeGram + Saturation + a cross-product XGCD completion; the choice affects speed only, never results (key is basis-fixed; every fibre is solved exactly). Worst observed pair after all this: 326.2 (the disc −163 deep-CM stratum) at 4.6 s; full maximal sweep 71 s CPU.
- 2026-07-22 (**VERIFICATION EVIDENCE — all green**):
  * `tests/run_quick.m`: 0 failures, 0 skips (twice), including the new T08 smoke block (determinism assert, Pollack counts, negation pairing, deg-5 non-divisor false, discO=30 deg-30 normalizing regression, Legacy-flag validity).
  * **Determinism sweep** (`tests/sweep_polarized_canonical.m`): all maximal D ≤ 999, every d | D — run TWICE in fresh processes: outputs **byte-identical**; 804 (D,d) with μ, 604 without, **0 Aut errors**, 71.1 s CPU per run. Every row asserted: μ ∈ O, μ² + d·disc = 0, μ normalizes O, DegreeOfPolarizedElement = d; IsTwisting and Aut ran on every μ.
  * **Existence pattern = formula**: the 804 (D,d) pairs with μ are EXACTLY the 804 rows of the shipped maximal file (label sets identical) — the class-number formula reproduces the shipped existence pattern perfectly, while the shipped `.m` file (800 rows) is missing 4 of them (see 800-vs-804 above).
  * **Class-count validation**: every `PolarizedElementClasses` call is internally validated against the independent Rotger/Eichler count (total AND per-conductor distribution asserted, plus negation-pairing and involution asserts); exercised across the battery (D=6/10/15 all degrees, Eichler discO=30/210 including 20- and 12-class cases), the audit's 397 jumped-row class enumerations, and the Eichler degree sweep (below). Literature anchors: D=6,10 → 1 principal polarization, D=15 → 2 (matches Rotger/known); paper's big-D example corrected (1040 → 1024, see erratum note).
  * **Regeneration** (`tests/regen_polarized_orders.m`, bound 1000): both data files regenerated with canonical μ. Maximal: 804/804 rows, none missing, none extra. Eichler: all 86 old rows regenerated **plus 415 new rows** — the old file only covered a fraction of the discO < 1000, squarefree-coprime-level grid (an interrupted/failed legacy run; e.g. 447.894.1 exists mathematically but was absent); the regenerated file completes the grid.
  * **Invariant comparison** (`tests/compare_polarized_files.py` + per-row audit): deg_mu and nrd_mu identical on every common row. AutmuO_{size,label,is_cyclic} identical on ALL 493 rows whose stored μ is Pollack-equivalent to ±(canonical μ) — and different on exactly 104 rows (88 maximal + 16 Eichler), **every single one of which the audit independently classifies as "jumped"**: the stored μ lies in a DIFFERENT Pollack class (397 such rows of 890), and Aut_{±μ}(O) genuinely depends on the class (C2 vs C2² here). Zero unexplained invariant changes. This is the shipped-data manifestation of Q3: those 88+16 rows' AutmuO columns were artifacts of Embed's arbitrary class choice.
  * **Eichler acceptance**: deg-1 for every Eichler order in the full discO < 1000 grid via the regeneration (86 old + 415 new rows, orders reconstructed from the stored bases of quaternion-orders.txt); higher-degree sample via `tests/sweep_eichler_degrees.m` (discO ≤ 150, all squarefree d | discO: 137 (order,d) with μ, 127 without, 43 s, all asserts incl. normalizing and in-process determinism; orders with non-squarefree level in quaternion-orders.txt are explicitly skipped as out of formula scope).
  * **grep-proof (acceptance criterion 1)**: the only `Embed`/`InternalConjugatingElement` calls under `code/level-structure/` are inside `HasPolarizedElementOfDegreeLegacy` (quarantined, reachable only via `Al:="Legacy"`); other repo hits are `jacobian_decomp/indefinite.m` (CM-field embedding for newform matching — not polarized elements) and `hep_covering/` (not attached via spec).
- 2026-07-22 (**PROVISIONAL banner**): the regenerated `data/quaternion-orders/quaternion-{maximal,Eichler}-orders-polarized.txt` are **PROVISIONAL — pending T27 reload** (and pending the ⟐ class-index label decision + the T06-wave reconciled regeneration, since agent A is concurrently changing Gerby_gen semantics in LMFDBRowEntry and AutmuO_generators conventions on their branch — my regeneration used THIS branch's LMFDBRowEntry). Do not run label-keyed `update_from_file` against the current DB from these files. Copies of the audit and the invariant-comparison output are staged in `shimcurve_tickets/artifacts/` (T08-class-audit.txt, T08-invariant-comparison.txt). The legacy 800-row `quaternion-orders-polarized.m` was deliberately left untouched (stale; retire under T04 with the writer unification).
- 2026-07-22 (**CLOSING — status: review**). Branch `ticket/T08-polarized-element` (worktree /Users/roed/claude/shim-wt/T08, base = T29 97bef4f), commits **c33673a, 0eeaa9b, fbac0d3, b36a49c, 4bc8327**. Final determinism results landed after the entry above: regeneration run twice (second run with REGEN_SKIP_AUDIT), **both data files byte-identical between runs**; Eichler degree sweep run twice, **outputs identical**. All acceptance criteria met: (1) no Embed/InternalConjugatingElement call path in polarized-element computation (legacy quarantined behind the flag, grep-proof in the evidence entry); (2) determinism sweep byte-identical twice over D ≤ 999 all degrees + invariant comparison old-vs-new fully documented and fully explained (104/104 invariant diffs = audited class jumps); (3) Eichler: deg-1 succeeds for the entire discO < 1000 grid (86 legacy + 415 restored rows) + higher-degree sample (137 pairs). run_quick green. **Flags for David:** (a) the ⟐ class-ordering + label-cascade proposal above needs sign-off (coordinates with Q11/Q15/T27, frontend LABEL_RE); (b) the 88 maximal + 16 Eichler shipped rows with class-dependent AutmuO are wrong-by-ambiguity in the DB until the class index lands; (c) Rotger-paper erratum (π₀ = 1024 not 1040) — maybe tell Rotger; (d) agent-A items: enumerate-O.m duplicate EnumerateOmuTxt signature, nondeterministic QuaternionOrder(B,M) fallback in EnumerateOmuTxt, and the T07 gerbiness cross-check that deg-2 D=6 classes are all ζ₆-band (a ζ₄-band deg-2 row is impossible); (e) upstream issue #5 closing comment drafted above — NOT posted, per hard rules; nothing was pushed anywhere.

### Label-cascade proposal (deliverable 6 — ⟐ OPEN, David's decision; NOT implemented anywhere)

Since (O, deg) determines a SET of Pollack classes, `mu_label` needs a class index. Concrete proposal:

1. **Row semantics**: one `quaternion_orders_polarized` row per NEGATION PAIR {[mu], [−mu]} (= isomorphism class of degree-d polarizations by Rotger Cor 3.7 + the index argument), because every consumer of mu (Aut_{±mu}, enhanced semidirect groups, AL elements) sees mu only mod Q^x where mu ≡ −mu. Store the key-min of the pair's two canonical class representatives; add per-pair invariants (conductor/disc of S_mu, content) as columns. If instead a row per single class is wanted, everything below holds with i running over classes.
2. **Label shape**: unify to four components **`discB.discO.deg.i`** (maximal orders write discO = discB: `6.6.2.1`). Rationale: appending `.i` to the CURRENT mixed shapes collides — maximal `D.deg` + index = 3 parts = indistinguishable from today's Eichler `D.discO.deg` (e.g. `6.2.1` would be both "maximal deg 2 class 1" and "Eichler discO=2..."). All labels change under the T27 reload anyway. Alternative (rejected): Cremona letter suffix `6.2a` — compact but breaks the all-numeric dot-separated convention.
3. **Index assignment**: i = position in the canonical class order implemented in PolarizedElementClasses — (conductor of S_mu ascending, canonical key of representative ascending); pairs ordered by their key-min member. Deterministic, reproducible from (O-basis, deg) alone.
4. **Cascade**: gps_shimura `mu_label` and the curve label gain the component; frontend `LABEL_RE` (lmfdb/shimura_curves/main.py:58-61) widens; coordinate with Q11 names (note: even deg-1 needs the index — D=15 already has TWO principal-polarization pairs, so "the" principal curve X(15) is ambiguous until i is fixed) and Q15/T29 ordering; land at T27 reload time.
5. **Migration**: the regen audit maps each SHIPPED row's stored mu to its canonical class index (same/negation/jumped:i), so old rows relabel to their true class, not blindly to i=1.
6. **Deferred**: an `is_ample` flag distinguishing the polarization (index 0) member of each pair needs a fixed orientation/splitting convention (Rotger §5-6); curve data is sign-insensitive, so defer.

### Draft upstream issue #5 closing comment (deliverable 7 — do NOT post)

> Fixed on branch `ticket/T08-polarized-element` (commits c33673a..HEAD, pending merge).
>
> `HasPolarizedElementOfDegree` no longer touches `Embed`/`InternalConjugatingElement` (grep-proof: the only remaining calls are the quarantined `HasPolarizedElementOfDegreeLegacy`, reachable solely via the comparison flag `Al := "Legacy"`, plus unrelated CM-field embeddings in `jacobian_decomp/` and the unattached `hep_covering/`). The canonical algorithm works directly on the rank-3 trace-zero lattice O^0: solutions of nrd(mu) = d·disc(O) are stratified by content (gcd of coordinates, a conjugation invariant), each stratum is certified empty/nonempty by the Eichler/Rotger class-number formula BEFORE any search, and nonempty strata are enumerated in growing sup-norm rings by an exact discriminant-pruned conic fibration. The returned mu is minimal under a documented deterministic key, always normalizes O (the old code could return non-normalizing mu for Eichler orders, crashing IsTwisting downstream), and the whole thing is uniform in maximal/Eichler orders of squarefree level. Existence is decided by formula, not by search, so termination is certified and "returns false" now means "does not exist" rather than "gave up".
>
> Bonus: polarized elements of a given degree are not unique up to units — they form finitely many *Pollack conjugation classes* (Rotger, Crelle 561 (2003)). New file `code/level-structure/pollack-classes.m` provides `PollackClassCount` (closed-form count), `IsPollackConjugate` (exact effective test), `PolarizedElementClasses` (complete canonical representatives, cross-validated against the count on every call), `OptimalEmbeddingConductor`. The old data files were missing four mathematically-existing rows (33.1, 33.11, 33.33, 34.1 in quaternion-orders-polarized.m) — legacy-fragility casualties, restored by regeneration.
>
> Also in `IsTwisting`: the "not sure if this is enough when O is not maximal" TODO is resolved — nrd(chi) | disc(O) is only used as a necessary-condition prefilter and normalizer membership is explicitly checked for every candidate; the bare one-value `return false` that crashed two-value callers is fixed.
>
> Verification: full D ≤ 1000 maximal sweep (every squarefree degree) run twice in fresh processes with byte-identical outputs; class enumerations agree with the independent class-number formula across the sample range; the 86-row Eichler deg-1 range regenerates end-to-end from stored bases plus higher-degree samples; regression tests added to the quick suite.
