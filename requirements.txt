# The LMFDB is pip installable (see pyproject.toml, which is where the
# dependencies are now recorded); this file remains as the traditional
# entry point:
#     sage -pip install -r requirements.txt
# installs the LMFDB in editable ("develop") mode, together with its
# dependencies and development tools.
-e .[dev]
